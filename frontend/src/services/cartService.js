// Stubbed cart API calls to avoid external errors; the cart is stored locally.
export async function createCartItem() {
  return Promise.resolve(null)
}

export async function updateCartItem() {
  return Promise.resolve(null)
}
